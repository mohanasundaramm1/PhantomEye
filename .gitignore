# Airflow logs and state
airflow/logs/
airflow/benign/
airflow/pre_migration_exports/
airflow/airflow-webserver.pid
airflow/airflow-worker.pid
airflow/airflow.db

# Data partitions
bronze/
silver/
lookups/
ct/data/
ct/chk/
gold/
chk/
dlq/

# Environments and caches
.venv/
__pycache__/
.pytest_cache/
.DS_Store
.env
.cache-tldextract/

# ML artifacts
ml/tmp/
ml/models/
ml/reports/
ml/data/

console.log
.claude/worktrees/

# launchd supervisor runtime artifacts (logs + watchdog state). The plist
# *templates* under ops/launchd/ are tracked; the rendered logs/state are not.
ops/launchd/logs/

# Runtime audit-trail output from one-off repair/rescore scripts (product/
# repair_brand_matcher_clusters.py, product/rescore_ct_observations.py) --
# a local record of what a run did, not source, same category as the
# runtime logs above.
product/logs/
AUDIT.md
